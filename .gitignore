# local files
notes_to_apply.md
.tmp
*egg-info
.claude
tmp

# data
/data/
/assets/local/datasets/
*.tar.gz
*.tar
*.zip

# python
__pycache__ 
.pytest_cache
.vscode
.to_delete
.pym2aia_prefs.xml
/docs/_build/

# notebooks
*.ipynb diff=jupyternotebook

# analysis precompute artifacts
# Every notebook writes its numerical tables to a sibling `<notebook>_results/`
# directory. Those are regenerable from the precompute runner and are large, so they
# are kept local instead of versioned; see the analysis README for how to rebuild them.
*_results/
