"""
Package initialization tracking clean optimization criteria blueprints.
Triggers recursive auto-discovery reflection routing upon package load.
"""

import os
from .criterions_manager import CriterionsManager

# Execute self-discovery scans bounding registered criterion subcomponents
path_list = [os.path.dirname(__file__)]
name_root = __name__

## Trigger automated reflection imports for deep autoencoder loss functions
CriterionsManager.discover_criterions(path_list, name_root)