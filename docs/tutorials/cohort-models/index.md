# Cohort models

This group constructs models trained across multiple image contexts without
merging the underlying MSI files. The tutorial uses `example_1` and `example_2`
and preserves each sample's image-local identity.

```{toctree}
:maxdepth: 1

cohort_models_01_multi_image_models
```
